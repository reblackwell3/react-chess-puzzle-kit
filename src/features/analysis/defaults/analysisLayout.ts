import { AnalysisLayoutConfig } from '../core/analysisLayoutConfig';

/** Library default grid; hosts should pass their own {@link AnalysisLayoutConfig}. */
export const DEFAULT_ANALYSIS_LAYOUT: AnalysisLayoutConfig = {
  boardWidth: 480,
  sidebarWidth: 500,
  columnGap: 16,
};
